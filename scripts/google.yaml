vars:
  TEST_VAR: 123
subcommands:
  example:
    actions:
      - open: https://example.com
      - log: "Starting automation on example"
      - screenshot: "/mnt/c/Users/CaLu150/Downloads/example.png"
      - log: "Page loaded with var ${TEST_VAR}"

  google:
    actions:
      - open: https://google.com
      - log: "Starting automation on google"
      - screenshot: "/mnt/c/Users/CaLu150/Downloads/google.png"
      - log: "Page loaded with var ${TEST_VAR}"

