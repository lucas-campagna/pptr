{
  "name": "pptr-monorepo",
  "private": true,
  "version": "1.9.2",
  "workspaces": [
    "packages/*"
  ],
  "description": "Monorepo for pptr (CLI + core)",
  "scripts": {
    "bootstrap": "npm ci",
    "test": "jest --runInBand",
    "build": "echo \"Build individual packages with their scripts (see packages/pptr)\""
  },
  "devDependencies": {
    "jest": "^29.6.1"
  }
}
